node_modules/
.next/
.env.local
.env
*.log
.DS_Store

# In-app AI settings (API keys saved via the Settings panel — see lib/ai-settings.ts)
.ai-settings.local.json

# Local auth store (session secret + hashed user passwords, incl. the seeded
# master admin — see lib/auth.ts). Never commit this.
.auth-users.local.json

# Project Management data (Tool #2) — projects, tasks, budget, crew,
# equipment. Local, per-installation data — see lib/project-store.ts.
.projects-data.local.json

# Reusable templates (Settings & Templates tab) — e.g. Budget templates.
# Local, per-installation data — see lib/template-store.ts.
.budget-templates.local.json
